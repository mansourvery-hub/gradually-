/// Shared typed identifiers used across all domain packages.
library;

/// Stable identifier for a vocabulary item (E-07 shared representation).
typedef VocabId = String;

/// Stable identifier for a content item in the corpus.
typedef ContentId = String;

/// Stable identifier for a sentence within a content item.
typedef SentenceId = String;

/// Stable identifier for an SRS review card.
typedef CardId = String;
